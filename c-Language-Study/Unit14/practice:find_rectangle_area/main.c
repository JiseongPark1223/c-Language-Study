#include <stdio.h>

int main()
{
    int base = 20;
    int height = 16;
    int area;
    
    area = base * height / 2; //area에 base * height에 2를 나눈 값을 할당하도록 한다.
    
    printf("%d\n", area);
    
    return 0;
}