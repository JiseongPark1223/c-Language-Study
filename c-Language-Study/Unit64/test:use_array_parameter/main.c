#include <stdio.h>

void reverseArray(int arr[], int count) //2차원 배열을 매개변수로 이용하는 함수를 선언한다.
{                                                  //2차원 배열을 사용할때는 배열의 가로크기를 지정해준다.
    for (int i = count - 1; i >= 0; i--)
    {
        
    }
}

int main()
{
    int arr[10];
    
    for (int i = 0; i < 10; i++)
    {
        scanf("%d", &arr[i]);
    }
    
    prin(arr, n); //함수에 각 값들을 넣는다.

    return 0;
}